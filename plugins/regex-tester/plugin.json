{
  "name": "apphighway-regex-tester",
  "description": "Test regex patterns with captures and explanations",
  "version": "1.0.0",
  "mcpServers": {
    "apphighway-regex-tester": {
      "url": "https://apphighway.com/api/mcp/regex-tester",
      "headers": {
        "Authorization": "Bearer ${APPHIGHWAY_API_KEY}"
      }
    }
  }
}
