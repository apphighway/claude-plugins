{
  "name": "apphighway-json-to-csv",
  "description": "Convert JSON arrays to CSV format",
  "version": "1.5.0",
  "mcpServers": {
    "apphighway-json-to-csv": {
      "url": "https://apphighway.com/api/mcp/json-to-csv",
      "headers": {
        "Authorization": "Bearer ${APPHIGHWAY_API_KEY}"
      }
    }
  }
}
